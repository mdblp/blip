{
  "name": "tidepool-platform-client",
  "version": "0.3.0",
  "description": "Client-side library to interact with the Tidepool platform",
  "main": "tidepool.js",
  "scripts": {
    "mrproper": "rm -rf node_modules",
    "clean": "rm -rf dist",
    "build": "webpack",
    "test": "mocha --timeout 10000 --exit --check-leaks test/**/*.test.js || /bin/true",
    "security-checks": "npm audit --production --audit-level=high --registry=https://registry.npmjs.org/"
  },
  "repository": "git+https://github.com/mdblp/blip.git",
  "author": "Tidepool",
  "license": "BSD-2-Clause",
  "dependencies": {
    "async": "3.2.0",
    "lodash": "^4.17.15",
    "superagent": "5.2.2",
    "uuid": "8.1.0"
  }
}
